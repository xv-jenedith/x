/* Microinteracciones de acabado premium */
document.querySelectorAll('.gold-button').forEach(button=>button.addEventListener('pointerdown',event=>{const rect=button.getBoundingClientRect();button.style.setProperty('--ripple-x',`${event.clientX-rect.left}px`);button.style.setProperty('--ripple-y',`${event.clientY-rect.top}px`);button.querySelector?.('span');button.classList.remove('rippling');requestAnimationFrame(()=>button.classList.add('rippling'));setTimeout(()=>button.classList.remove('rippling'),700)}));
document.querySelectorAll('.event-card,.dress-detail,.gift-card').forEach(card=>{card.addEventListener('pointermove',event=>{if(innerWidth<700)return;const r=card.getBoundingClientRect(),x=(event.clientX-r.left)/r.width-.5,y=(event.clientY-r.top)/r.height-.5;card.style.transform=`perspective(900px) rotateY(${x*2}deg) rotateX(${y*-2}deg)`});card.addEventListener('pointerleave',()=>card.style.transform='')});
if(window.gsap&&window.ScrollTrigger){gsap.utils.toArray('.gift-card,.dress-detail,.gallery-grid').forEach(el=>gsap.from(el,{opacity:0,y:38,scale:.96,duration:1,ease:'power3.out',scrollTrigger:{trigger:el,start:'top 85%'}}));}
